---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-notification
  namespace: app
  labels:
    app: service-notification
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-notification
  template:
    metadata:
      labels:
        app: service-notification
        version: v1
    spec:
      serviceAccountName: service-notification-sa
      containers:
      - name: service-notification
        image: service-notification:1.0
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: service-notification
  namespace: app
spec:
  selector:
    app: service-notification
  ports:
  - name: http
    port: 8080
    targetPort: 8080
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: service-notification-sa
  namespace: app
